/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Ticket } from '../../models/ticket';

export interface ListTickets$Params {

/**
 * Missing/invalid → 400; a teamId that matches no team → 404
 */
  teamId: number;
}

export function listTickets(http: HttpClient, rootUrl: string, params: ListTickets$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<Ticket>>> {
  const rb = new RequestBuilder(rootUrl, listTickets.PATH, 'get');
  if (params) {
    rb.query('teamId', params.teamId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<Ticket>>;
    })
  );
}

listTickets.PATH = '/tickets';
